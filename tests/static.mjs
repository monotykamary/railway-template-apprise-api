import{readFileSync}from"node:fs";import assert from"node:assert/strict";const d=readFileSync("Dockerfile","utf8"),e=readFileSync("entrypoint.sh","utf8"),r=readFileSync("railway.toml","utf8");assert.match(d,/apprise:v1\.5\.1@sha256:[a-f0-9]{64}/);assert.doesNotMatch(d,/:latest/);assert.match(e,/auth_basic/);assert.match(e,/openssl passwd -apr1/);assert.match(e,/unset APPRISE_AUTH_PASSWORD/);assert.match(e,/location = \/status/);assert.match(r,/healthcheckPath = "\/status"/);console.log("static template checks passed");
